# ydg-order-stock

Spring Boot + React

## Structure
- `backend/` Spring Boot 3.x (Java 21)
- `frontend/` React + Vite
- `e2e/` Selenium + JUnit
- `docker-compose.yml` app + e2e profile
- `Jenkinsfile` pipeline stages

